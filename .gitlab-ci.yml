image: alpine

pages:
  script:
  - apk --no-cache add py3-pip python3-dev
  - pip install sphinx
  - apk --no-cache add make
  - cd docs
  - make html
  - cd ..
  - mv docs/_build/html/ public/
  artifacts:
    paths:
    - public
