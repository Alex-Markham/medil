image: alpine

pages:
  script:
  - apk --no-cache --update add pip python-dev
  - pip install sphinx
  - apk --no-cache add make
  - cd docs
  - make html
  - cd ..
  - mv docs/_build/html/ public/
  artifacts:
    paths:
    - public
